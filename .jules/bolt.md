## 2024-05-19 - Zustand useWorldState Causes N+1 Re-Renders in List Item Components
**Learning:** `useWorldState()` subscribes to the entire global state object by default. When used in a component that is rendered inside a large list (like `FormSparkline` in a roster table), it completely breaks `React.memo()`. Any unrelated global state change (e.g., ticking the week, treasury changing) will trigger a re-render for every single item in the list, causing massive O(N) performance regressions.
**Action:** In small list components, never call `useWorldState()` directly. Instead, extract exactly what you need using a narrow selector like `useGameStore((s) => s.specificSlice)` to maintain `React.memo` isolation. Only use `useShallow` if the selector returns a newly constructed object or array.

## 2024-06-02 - EntityLink re-rendering optimization
**Learning:** `WarriorLink` and `StableLink` components were using `useWorldState()` to fetch global game state just to resolve names to IDs. This caused them to re-render constantly on every game state change, even when their target warrior/stable wasn't affected. Since these components are used everywhere (e.g. `EventLog`, `WarriorAuditCard`), it created a massive performance bottleneck, triggering re-renders across the app.
**Action:** Replace `useWorldState()` with `useGameStore(useShallow(...))` to only subscribe to the strictly necessary state slices (`player`, `rivals`, `roster`, `graveyard`, `retired`) needed for name resolution.
## 2024-06-03 - Replacing useWorldState with useShallow

**Learning:** When replacing full-state subscriptions (`useWorldState()`) with narrowed `useShallow` selections from `useGameStore`, remember that many game state properties (e.g., `roster`, `insightTokens`, `retired`) are optional and may be undefined. Always preserve or re-implement nullish coalescing fallbacks (e.g., `const safeRoster = roster ?? []`) before calling array methods like `.map` or `.find` to prevent runtime crashes. Additionally, when renaming variables for these safe fallbacks (e.g., `safeRoster`), make sure to update *all* references within the component. Finally, ensure any bound functions or actions retrieved from the store (e.g., `consumeInsightToken`) are included in the shallow selector or retrieved separately to avoid inadvertently causing the component to subscribe to the entire state again.

**Action:** Whenever refactoring `useWorldState()` to `useShallow`, I must specifically check for array operations (like `.find()` or `.map()`) and verify that default values (`?? []`) are applied *before* the operations. I will also double-check that actions (e.g., `consumeInsightToken`) are properly extracted alongside state properties without triggering global re-renders.

## 2024-05-18 - Replacing `useWorldState` with `useShallow` for Performance
**Learning:** In the `useGameStore`, `useWorldState()` is a global subscription that causes components to re-render whenever *any* part of the global state changes. Several widget and UI components were using this unnecessarily.
**Action:** Replace `useWorldState()` with `useShallow()` and explicitly select only the state properties needed by the component. This drastically reduces unnecessary re-renders in highly dynamic parts of the application (like dashboards and overlay components).
## 2026-06-08 - Replacing useWorldState with useShallow safely\n**Learning:** When refactoring components to use `useShallow` instead of `useWorldState`, it is critical to extract properties from their correct root-level locations in the Zustand store (e.g., `s.unacknowledgedDeaths`), rather than blindly assuming their paths. If these properties are `undefined`, accessing indices or invoking array methods will result in fatal runtime crashes. Always provide nullish coalescing default arrays (e.g., `?? []`) for safety.\n**Action:** Verify the target properties' structure inside `src/state/useGameStore.ts` and `src/state/slices/` before extracting them using `useShallow`. Always apply `?? []` fallbacks to ensure safe iteration or index access.
## 2026-06-13 - StableDossier Optimization
**Learning:**  component was re-rendering unnecessarily on any global state change due to using `useWorldState()`. This was inefficient since it only needs access to `player`, `roster`, and `rivals`.
**Action:** Replace `useWorldState()` with a narrowed `useGameStore(useShallow(...))` selector to improve performance by limiting re-renders.
## 2024-05-19 - StableDossier Optimization
**Learning:** `StableDossier` component was re-rendering unnecessarily on any global state change due to using `useWorldState()`. This was inefficient since it only needs access to `player`, `roster`, and `rivals`.
**Action:** Replace `useWorldState()` with a narrowed `useGameStore(useShallow(...))` selector to improve performance by limiting re-renders.
## 2025-02-12 - Replaced useWorldState() with Narrow useShallow Slice
**Learning:** In Zustand stores, large full-state subscriptions like `useWorldState()` cause components to re-render anytime *any* property of the global state updates (e.g. week advancing, treasury changes). Using `useShallow` with narrowed selectors prevents this. However, passing these narrow state slices to downstream helper hooks (that previously expected the full `GameState` interface) using a type assertion (`state as GameState`) is an anti-pattern that can cause runtime crashes if the helper attempts to access un-sliced properties.
**Action:** When extracting a narrow slice, always explicitly update the helper functions to accept the narrower interface (e.g. `Pick<GameState, 'roster' | 'week'>`) rather than using unsafe type casts. This ensures type safety and prevents hidden bugs.

## 2024-06-17 - Dangerous Fallbacks Inside Zustand's useShallow Selector
**Learning:** When narrowing Zustand `useWorldState()` hook calls to use `useShallow`, attempting to provide a default fallback directly inside the selector body (e.g., `useShallow((s) => ({ roster: s.roster ?? [] }))`) is a dangerous performance anti-pattern. If `s.roster` is `undefined`, `?? []` constructs a brand new empty array reference in memory every single time the state store evaluates. This causes `useShallow`'s reference-equality check to fail on every single store change (even completely unrelated ones), breaking memoization and triggering an infinite storm of re-renders or totally negating the performance optimization.
**Action:** Never place fallback allocations (`?? []`, `?? {}`, or `|| []`) inside the `useShallow` arrow function. Always extract the property as-is (`roster: s.roster`), and apply the nullish coalescing default later in the component body (e.g., `const safeRoster = worldState.roster ?? []`).

## 2024-06-25 - Object Creation Inside Zustand's useShallow Defeats Memoization
**Learning:** When using Zustand's `useShallow`, its internal comparison logic only checks for equality at a single shallow level (e.g., comparing array elements by reference). If you write a `useShallow` selector that constructs a *new array of new objects* (e.g., `s.roster.map(w => ({ id: w.id }))` or manually pushing objects into an array), the selector returns new object references on every single evaluation. Because the inner objects are new, `useShallow` correctly determines the arrays are "different" and fails to memoize, triggering re-renders across the app for completely unrelated state changes (like the week advancing).
**Action:** Never map state to a new array containing *new objects* inside a `useShallow` selector. If you just need primitives (like IDs or names), `useShallow((s) => s.roster.map(w => w.id))` is safe because primitive values compare successfully. For objects, extract the required state array directly (`const roster = useGameStore(s => s.roster)`) and then use `useMemo` outside the hook to derive your new array of objects.
