# Orphan Feature Audit — Stable Lords

> Generated by the orphaned-feature megaplan (Phases 0–2). Every claim verified
> against source at audit time. Baseline @ `49802cb2`: type-check 0 errors,
> lint 0/0, **7,590 tests / 631 files** green (2 skipped), scan tooling:
> `scripts/orphan-scan.mjs` → `scripts/out/orphan-scan.json`.
>
> Verdicts: **APPROVED** (confirmed orphan), **CORRECTED** (real but different),
> **DISPROVED** (premise false — no action), **NOTE** (intentional-dormancy
> candidate — still surfaced for sign-off).
>
> **Scope note:** Prior AI gap register (`docs/ai-behavior-audit.md` G1–G21) was
> re-verified — all findings confirmed **implemented** (perception snapshot,
> intel worker, tournament worker, poach market, season records, plan
> persistence, player-bound bids all live in `RivalStrategyPass`). None carried
> forward as open.

---

## A. Whole-module orphans (unreachable or test-only)

| # | Finding | Class | Verdict | Evidence | Wiring plan |
|---|---------|-------|---------|----------|-------------|
| A1 | Parallel tournament engine — `matchmaking/tournament/{tournamentBracketBuilder,tournamentResolver,tournamentStateMutator}.ts` (590 lines) | Loop | **APPROVED — superseded** | Zero non-test importers; live path is `matchmaking/tournamentSelection/resolution.ts` (`resolveRound` via `TickOrchestrator`). `warriorLookup.ts` mentions the old path only in a comment. | Escalate: dead parallel implementation of a live system. Options: (a) port any unique logic (arena eligibility via `tournamentArenaSelection.getEligibleArenasForTournament` — also dead) into `tournamentSelection` then remove files, or (b) rewire `resolveRound` onto this engine if it's the intended design. Needs your call. |
| A2 | `engine/training/burnAnalysis.ts` — `assessBurnRisks(warrior, trainers) → BurnWarning[]` | UI + AI | **APPROVED** | 48-line burn-warning engine; only consumer is `src/test/engine/training/burnInjury.test.ts`. Spec'd as Feature Matrix #8 "Trainability & Burns Advisor". | Wire: (C) surface warnings on `/stable/training` + `/stable/planner` per warrior row; (B) feed into rival `rosterWorkerTraining` so AI stables avoid burning their own warriors. |
| A3 | `engine/ai/plan/tacticAdvisor.ts` — `getBestOffensiveTactic`/`getBestDefensiveTactic` (suitability + payoff-ranked) | AI | **APPROVED** | Test-only. Live path uses the hardcoded `getAITactics` switch (`ai/plan/levers.ts:108`) — the advisor is strictly smarter (ranks by `tacticSuitability` then actual combat payoff via `tacticResolution` mods). | Wire (B): route `aiPlanForWarrior`/plan generation through the advisor (replace or shadow the hardcoded switch, keeping preset-table parity where intended). Also fixes `ai/plan/index.ts` barrel (test-only). |
| A4 | `components/run-round/{BoutRow,OutcomeIcon,RunResults,RunResultsSummary}.tsx` | UI | **APPROVED — superseded?** | Test-only component subtree rendering bout results; live result display is `ResolutionReveal` (`__root.tsx:12,56`). Siblings `AutosimConsole`, `LethalityBadge`, `MatchCard` are wired. | Escalate: either rewire into a results view (e.g., `/stable/bouts` detail) or delete as superseded — your call under wire-everything, recommend salvage `OutcomeIcon`/`LethalityBadge`-style glyphs if ResolutionReveal lacks them. |
| A5 | `components/arena/effects/WeaponTrail.tsx` | UI | **APPROVED** | Zero importers anywhere (not even tests). Animated weapon-trail effect for the bout viewer. | Wire (C): mount in `components/arena/effects/` chain of `BoutViewer` if the spatial viewer supports it; else escalate (may predate the current viewer). |
| A6 | `components/ui/{StatCard,HeaderMetricDisplay}.tsx` | UI | **APPROVED** | Zero importers. StatCard = variant-mapped stat tile; HeaderMetricDisplay = titled metric strip — both match bible §7 primitives. | Wire (C): adopt in dashboards where stat tiles are hand-rolled (Control Center, Stable Hall metrics) — or delete if primitives were superseded by `Surface`/badges. |
| A7 | `constants/core/saveVersion.ts` — dead barrel | Loop | **REMOVED** | Re-exports `SAVE_STATE_VERSION` from `./core`; consumers import `@/constants/core` directly. | Deleted — barrel had zero consumers. |
| A8 | `engine/factories/combatFactory.ts` — `makeFightSummary` test helper | Loop | **REMOVED** | Test-only factory living in `engine/factories/`; duplicated by `_fixtures/factories.ts::makeFightSummary`. | Deleted; the 2 test consumers repointed to the fixture factory; `combatFactory.test.ts` removed (fixture has its own coverage). |
| A9 | `utils/storage.ts` — `handleLocalStorageQuotaError` | UI | **WIRED** | Test-only quota-retry helper; localStorage call sites each had hand-rolled copies. | Wired into `LoreArchive.saveArray` + `arenaHistory.save` (deduped ~40 lines). Helper tightened to only trim-retry on `QuotaExceededError` (non-quota errors rethrow). Call-site tests updated to helper semantics (trim incoming array's oldest 20%). |
| A10 | `components/{bookmarks,planBuilder}/index.ts` dead barrels | UI | **REMOVED** | Barrels unimported; consumers deep-import the components. | Deleted — zero importers. |

## B. Dead state fields

| # | Field | Verdict | Evidence | Wiring plan |
|---|-------|---------|----------|-------------|
| B1 | `Warrior.seasonPoints` | **APPROVED** | Declared `types/warrior.types.ts:170` + `warriorSchemas.ts:248`; zero writers, zero readers. | Escalate: either implement (season-long points race feeding leaderboards/awards — matches awards system) or remove field. Likely a vestige of the points-for-rankings mechanic; recommend wiring into `ProgressionPass`/rankings or dropping. |
| B2 | `Owner.ageRetired` | **APPROVED** | `state.types.ts:155` "Week the previous owner retired" + `economySchemas.ts:70`; never written or read. | Wire: set in `handleOwnerLifecycle` succession (`RivalStrategyPass.ts:243`) and surface in stable dossier/history — or remove. |

## C. Dead data / narrative content

| # | Finding | Verdict | Evidence | Wiring plan |
|---|---------|---------|----------|-------------|
| C1 | `uiMeta.persona` (`good`/`bad`/`descriptors`) | **WIRED** | `uiMeta.json`; loaded eagerly into `narrativeContent.persona`; zero `getFromArchive`/`peekArchive`/direct reads. | Wired into `generateScoutReportNotes` — detailed/expert scout reports emit a persona-flavored qualitative line keyed to the warrior's strongest attribute; basic reports stay generic. Test: `src/test/engine/scouting.test.ts`. |
| C2 | `uiMeta.meta` (`flair`/`title`/`injury`/`status`) | **DISPROVED** | Scanner false positive — `src/data/tagDescriptions.ts` aliases `uiMeta.meta` into `FLAIR_/TITLE_/INJURY_/STATUS_DESCRIPTIONS`, rendered as `TagBadge` tooltips (`WarriorBadges.tsx`). | Already wired; no change needed. |
| C3 | `data/arenas.ts :: ARENA_LORE` + `getArenasByTag` | **WIRED** | ~20+ `ArenaLoreEntry` records + tag query, declared `arenas.ts:48,505`; zero consumers. | Wired: `ARENA_LORE` rendered on `ArenaLeaderboards` for the selected venue (title+narrative, real `ArenaLoreEntry` fields only); `getArenasByTag` consumed by `arenaFit.selectArenaForMatchup` — severe weather filters to indoor arenas. Tests: `src/test/pages/ArenaLeaderboards.test.tsx`, `src/test/engine/matchmaking/arenaFit.test.ts` (weather suite). |
| C4 | `data/ownerData.ts :: META_RECRUIT_QUOTES` | **WIRED** | Zero consumers. | Wired into `DoctrineIntelligenceSection` — the rival's `metaAdaptation` quote renders beneath the Adaptation row in StableComparison. Test: `src/test/components/scouting/DoctrineIntelligenceSection.test.tsx`. |
| C5 | `data/terrabloodCharts.ts :: ENCUMBRANCE_LABELS` | **WIRED** | Zero consumers (siblings in file are used). | Wired into `WarriorDossierStats` — Combat Measures grid shows a Capacity card (`computeEncumbranceClass(ST,CN)` → class letter + canonical label). Test: `src/test/components/warrior/dossier/WarriorDossierStats.test.tsx`. |

## D. Dead exports inside live modules (verified zero references)

| # | Export | Module | Verdict | Wiring plan |
|---|--------|--------|---------|-------------|
| D1 | `applyCareerStats`, `applyFameDelta` | `engine/bout/warriorStateUpdater.ts` | **APPROVED** | Verify whether career-stat/fame application was inlined elsewhere; if duplicated, route through these; else remove. |
| D2 | `processOutcomeTags` | `engine/combat/mechanics/simulateHelpers.ts` | **APPROVED** | Check whether outcome tags are consumed anywhere; if bout outcomes lose tag metadata, wire into `postFight`/logging. |
| D3 | `pickText` | `engine/combat/mechanics/combatMath.ts` | **APPROVED** | Text-picker helper — likely superseded by `getFromArchive`; remove or wire. |
| D4 | `getFatigueBand` | `engine/core/fatigueUtils.ts` | **APPROVED** | Wire into fatigue display (UI) or AI rest decisions. |
| D5 | `createMinimalFightSummary` | `engine/core/fightSummaryFactory.ts` | **APPROVED** | Check consumers of full factory; likely test-support — move or remove. |
| D6 | `clearHistoryResolverCaches`, `clearWarriorCache` | `core/historyResolver.ts`, `core/warriorLookup.ts` | **NOTE** | Cache-hygiene exports for tests; intentional. |
| D7 | `getFightsForArena`, `getFightsForTournament` | `engine/core/historyUtils.ts` | **APPROVED** | Wire into arena/tournament history views (Chronicle, Arena detail). |
| D8 | `calculateArenaLeaderboard` | `engine/core/leaderboards.ts` | **APPROVED** | Superseded by `calculatePerArenaLeaderboards` (used by `ArenaLeaderboards.tsx`); remove or wire. |
| D9 | `collectAvailableWarriors`, `collectHealthyWarriors`, `countActiveWarriors` | `engine/core/warriorCollection.ts` | **APPROVED** | Wire into AI roster/booking availability checks or remove as duplicated helpers. |
| D10 | `getRandomCharge`, `getCrestColor`, `getCrestDescription` | `engine/crest/*` | **APPROVED** | Crest-description helpers — wire into stable dossier/crest display UI. |
| D11 | `CROWD_MOODS` | `engine/crowdMood.ts` | **APPROVED** | Constant list unused — check if mood iteration needed (UI mood picker/history). |
| D12 | `generateSeasonSummary` | `engine/gazette/gazetteNarrative.ts` | **APPROVED** | Wire into offseason/season-recap gazette (bible Annual Cycle). |
| D13 | `applyHealthUpdates` | `engine/health.ts` | **APPROVED** | Legacy convenience wrapper around `computeHealthImpact`; dead since impact pipeline. Remove or wire. |
| D14 | `countInjuries`, `getInjurySeverityCounts`, `hasInjuries`, `hasInjuryOfSeverity` | `engine/injuries/utils.ts` | **APPROVED** | Wire into injury badges/roster UI + AI booking checks. |
| D15 | `NewsletterFeed` | `engine/newsletter/feed.ts` | **APPROVED** | Whole exported object dead — check whether fight-card feed is superseded by `newsletter` state field; escalate. |
| D16 | `getWeatherSeason` | `pipeline/passes/WorldPass.ts` | **APPROVED** | Internal-looking helper exported; wire or unexport. |
| D17 | `formatPulseTable` | `engine/stats/simulationMetrics.ts` | **APPROVED** | Wire into sim-harness/telemetry output or admin telemetry panel. |
| D18 | `SUITABILITY_LABELS`, `SUITABILITY_COLORS` / `WEAPON_SUITABILITY_*` | `tacticSuitability.ts`, `weaponSuitability.ts` | **APPROVED** | UI display metadata for suitability ratings never rendered — wire into PlanBuilder/TacticBank + equipment UI (spec: player transparency on style×tactic fit). |
| D19 | `getTelemetryProvider`, `setTelemetryProvider` | `engine/telemetry.ts` | **NOTE** | Provider registration API — intentional extension point; keep. |
| D20 | `isRetired` | `engine/warriorStatus.ts` | **APPROVED** | Wire into status checks or remove. |
| D21 | `setMockIdGenerator`, `clearDeathHandlers` | `idUtils.ts`, `deathNotifier.ts` | **NOTE** | Test hooks — intentional. |
| D22 | `getWarriorPairKey`, `addCapped`, `roundTo`, `stringToSeed` | `utils/*` | **APPROVED** | Dead utilities — wire where the pattern recurs or remove. |
| D23 | `filterActive`, `filterByStatus`, `removeFromRoster`, `updateRoster` | `utils/roster.ts` | **APPROVED** | Dead roster helpers — wire into roster-management paths (many sites hand-roll filters) or remove. |
| D24 | `opfsArchive` | `engine/storage/opfsArchive/index.ts` | **NOTE** | Singleton export; all persistence routes through `archiveService` (F-arch1) — intentional facade residue. |
| D25 | `OWNER_PERSONALITIES_WITH_POLICY` | `ai/traitPolicy.ts` | **APPROVED** | Dead const (TRAIT_POLICY itself is used via `policyFor`). Remove or wire into personality tooling. |
| D26 | `StableLinkSheet`, `WarriorLinkSheet` | `components/EntityLink.tsx` | **APPROVED** | Sheet components never rendered — wire into entity-link UX or remove. |
| D27 | `BronzeHighlight`, `ChampionDisplay`, `TournamentProgress` | `components/tournaments/TournamentBracket.tsx` | **APPROVED** | Sub-components never rendered — wire into bracket display (champion callout, progress strip). |
| D28 | `RiskLevel` type + misc prop-type exports | various | **NOTE** | Type-only exports — not orphans, API surface. |

## E. Nav-hidden routes (routed but unreachable via UI)

| # | Route | Verdict | Evidence | Wiring plan |
|---|-------|---------|----------|-------------|
| E1 | `/admin` → `pages/AdminTools` | **WIRED** | Full admin console (CategoryNav, SystemPanel, WorldPanel, EconomyPanel, TelemetryPanel) routed, zero nav links. Matrix #39 "Admin Tools". | Wired: `UTILITY_LINKS` strip in `navigationHubs.ts` rendered at bottom of `LeftNav`/`MobileNav`; admin entry dev-gated via `import.meta.env.DEV`. Test: `src/test/components/navigationHubs.test.ts`. |
| E2 | `/lore/hall-of-fights` → `lore/HallOfFights` | **WIRED** | Routed, no nav link. | Wired: added to World hub pages as "Hall of Fights". |
| E3 | `/tools/physicals-simulator` → `pages/PhysicalsSimulator` | **WIRED** | Routed, no nav link. Matrix #9 "Physicals Simulator". | Wired: added to Stable hub pages as "Simulator". |
| E4 | `/help` | **WIRED** | Reachable via `?` keyboard shortcut only (`useKeyboardShortcuts.ts:62`). | Wired: visible Help link in the `UTILITY_LINKS` strip (always shown, not dev-gated). |
| E5 | `/arena-hub` | **DISPROVED** | Duplicate alias of nav-linked `/stable/arena` (both render `ArenaHub`). | No action — redundant alias, not an orphan. |
| E6 | `/welcome`, param routes (`/warrior/:id`, `/stable/promoter/:id`, `/world/stable/:id`) | **DISPROVED** | FTUE flow + entity-deep-links via EntityLink. | No action. |

## F. AI-unreachable subsystems (player-only — review whether intentional)

| # | Module | Verdict | Evidence | Wiring plan |
|---|--------|---------|----------|-------------|
| F1 | `stableReputation` (reputation quadrants) | **WIRED** | `computeRivalReputation` was UI-only (`useQuadrantDots`); no AI/economic consumer. | Wired `40c31eb4`: notoriety now inflates `convertBidsToOffers` offer hype (killer stables draw crowds); `styleMatchupGate`/notoriety tests in `boutBiddingIntegration.test.ts`. |
| F2 | `scoutInsights` | **NOTE** | `generateScoutInsights` consumed by `scouting.ts:195` (`generateScoutReport`); rival intel is structured dossiers (`intelDossier.ts`), surfaced via `RivalIntelligence` dossier count. No prose-insight slot in `OpponentDossier` — reuse would be artificial. | Closed — player-scout prose vs rival dossiers is intentional divergence; module is fully wired on its own path. |
| F3 | `stats/{cumulativeTracker,styleRollups,stableStats}` | **NOTE** | `calculateStableStats` consumed by `useScoutingStableComparison`/`stableStats(rival)` (rival rollups DO exist in scouting UI); `StyleRollups` consumed by `createStore`; `cumulativeTracker` is harness instrumentation by design (survives array truncation for `daily_oracle`). | Closed — no orphan; gazette "top stable" suggestion overlaps existing SystemPass points-race line. |
| F4 | `fightForecast` | **WIRED** | UI consumer already existed (`OfferCard` → `FightForecastPanel`); no AI consumer. | Wired `2a58822b`: `evaluateBoutOffer` declines strongly unfavorable style matchups (`edge <= -2`) for Methodical/Pragmatic stables; `styleMatchupGate.test.ts`. |
| F5 | `deathNotifier` | **NOTE** | Player-facing notification service. | Intentional. |
| F6 | `restStates` for rivals | **NOTE** | G19 — documented intentional (rival rest expressed via bid suppression). | No action. |
| F7 | `recruitScouting` | **NOTE** | Player recruit-scouting; rivals draft directly (G9 has separate path). | Intentional divergence per design decision log. |
| F8 | `health.ts` for rivals | **DISPROVED** | Rivals get fatigue decay (`stableManager.ts:40`) + injury ticks (`rosterWorker.ts:48-57`) via their own path. | No action — parallel impl, not a gap. |

## G. Spec-declared but unimplemented (Feature Integration Matrix gaps)

| # | Matrix row | Verdict | Evidence | Wiring plan |
|---|-----------|---------|----------|-------------|
| G1 | #25 House Rules / Mods Loader | **IMPLEMENTED** | `GameState.houseRules` (deathRateMult, severeInjuryInsteadOfDeath) optional field, schema-validated. | `deathRateMult` scales kill-window threshold in `hitExecution.ts` (threaded `simulateFight`→`initializeResolutionContext`→`ResolutionContext`); both call sites pass `state.houseRules` (`boutResolution.ts`, tournament `resolution.ts`). `severeInjuryInsteadOfDeath` intercepts `handleDeath` — fatal blows become Critical injuries, no graveyard entry. `/mods` page with spec-required non-canonical labeling; utility-strip nav link. Tests: `src/test/engine/houseRules.test.ts`, `src/test/pages/Mods.test.tsx`. |
| G2 | #27 Import/Export Manager (JSON/YAML packs) | **IMPLEMENTED** | `/import-export` route + page; `yaml@2.9.1` dep. | `src/lib/importExport.ts` — pack envelope, JSON/YAML serialize+parse, `GameStateSchema` validation (added missing `absoluteWeek` field the audit exposed). Utility-strip nav link. Tests: `src/test/lib/importExport.test.ts`, `src/test/pages/ImportExport.test.tsx`. |
| G3 | #31 Theme/Accessibility Pack (high-contrast, text size) | **IMPLEMENTED** | `motion-reduce` honored; no high-contrast/text-size controls found. | `src/lib/a11yPrefs.ts` (localStorage-persisted contrast + text-scale, applied via `data-contrast` + `zoom` on documentElement at boot in `main.tsx`); `.high-contrast` token overrides in `index.css`; Accessibility section on Help page. Tests: `src/test/lib/a11yPrefs.test.ts`, `src/test/pages/HelpA11y.test.tsx`. |
| G4 | #33 Onboarding Quests & Tips | **IMPLEMENTED** | `useCoachTip` exists; quest structure absent. | `src/engine/onboarding/quests.ts` derives quest completion from live GameState (no schema change; dismissal via `coachDismissed`); `QuestsWidget` on Control Center overview. Tests: `src/test/engine/onboarding/quests.test.ts`, `src/test/components/dashboard/QuestsWidget.test.tsx`. |
| G5 | #34 Design Bible Search | **IMPLEMENTED** | `MarkdownReader` was live only inside GazetteArticle. | `src/lib/bibleIndex.ts` loads all `docs/*.md` via `import.meta.glob ?raw`, splits into heading sections, ranked snippet search; `BibleSearch` accordion section on Help page. Tests: `src/test/lib/bibleIndex.test.ts`, `src/test/pages/HelpA11y.test.tsx`. |
| G6 | #15 Kill Analytics (Mechanics of Death visualizer) | **IMPLEMENTED** | `exchangeLog` is bout-time only (not persisted); persisted kill telemetry = `FightSummary.by/isDeathEvent/deathEventData.memorialTags/analysis.decisiveExchange` + graveyard `causeOfDeath`/`killedBy`. | `src/engine/analytics/killAnalytics.ts` aggregates kills/kill-rate, cause buckets, killer styles, fatal-minute bands, memorial tags, top killers; `KillAnalyticsPanel` rendered as "Mechanics of Death" tab in `GraveyardTabs`. Tests: `src/test/engine/analytics/killAnalytics.test.ts`, `src/test/components/fallen/KillAnalyticsPanel.test.tsx`. |
| G7 | #36 Content Updater | **IMPLEMENTED** | `GameState.contentPacks` — validated JSON packs persisted with the save. | `src/lib/contentPacks.ts` — zod-validated packs overlaying arena lore + doctrine recruit quotes; consumed by `ArenaLeaderboards` (`getPackArenaLore`) and `DoctrineIntelligenceSection` (`getRecruitQuote`); install/remove UI on `/mods` page. Tests: `src/test/lib/contentPacks.test.ts`, `src/test/pages/Mods.test.tsx`. |

## H. Disproved premise checks (verified wired — no action)

| Claim | Status |
|-------|--------|
| `poachBid.ts` dead | **Wired** — `processPoachMarket` runs in `RivalStrategyPass:182`; only type/helper exports unimported |
| `agentPlan.ts` dead | **Wired** — `persistNPCPlans` imported by `RivalStrategyPass:23` |
| `distanceResolution.ts` dead | **Wired** — consumed by `offenseDefense`, `exchangeSubPhases`, `simulate/initialization`, `arenaFit`, `ArenaLeaderboards` (ARENA_SIZE_PROFILES) |
| `intentEngine` exports dead | **Wired** — `pickWeeklyIntent`/`verifyIntentSkepticism`/`intentStillApplies` called inside `updateAIStrategy` |
| `decayDossiers`, `preferredTrainerFocus`, `projectedWeeklyUpkeep`, `MAX_PLAYER_OFFERS_PER_RIVAL`, `isPoachingEnabled`, `extractWeekSummary`, `createDefaultMeta`, `deriveBoutIntent`, `scoreArenaFitForWarrior`, `resolveAbsoluteWeek`, `processPlayerOffers` dead | **Wired internally** — used within their own modules; exports are API surface, not orphans |
| `counterBoutOffer`/`COUNTER_PURSE_MULTIPLIER` dead | **Wired** — `contractMutations` consumed by `worldSlice/boutActions`, `useBookingOffice`, `boutAcceptance`, `offerProcessor`, `autosim` (G7 implemented) |
| Rival warriors get no injury/fatigue processing | **DISPROVED** — `stableManager` + `rosterWorker` handle rival health |
| `traitData`/`traitDefs` traits unapplied | **DISPROVED** — generic `effect.*` consumption in `traitMods`; `feintTendency`/`attrBonus`/`fightPlanMod` all consumed |
| ai-behavior-audit G1–G21 open | **RESOLVED** — all findings show implementation markers (B.1, D.5, D.7, E.2, G8, G11, G18 fixes in-tree) |

## Summary counts

| Bucket | Count |
|--------|-------|
| Whole-module orphans (A) | 10 (7 approved, 3 note) |
| Dead state fields (B) | 2 |
| Dead data (C) | 5 |
| Dead exports in live modules (D) | 28 rows (~45 symbols; ~20 approved, rest note) |
| Nav-hidden routes (E) | 3 approved + 1 note (+2 disproved) |
| AI-unreachable (F) | 2 approved-candidates, 6 notes/disproved |
| Spec gaps (G) | 7 (all implemented: G1–G7 wired with production-path tests) |

**Escalation list (wire-everything conflicts):** RESOLVED — A1 removed (superseded tournament engine), A4 salvaged into `BoutsStep`, B1 seasonPoints wired, D15 NewsletterFeed wired, G1/G2/G4/G7 built as scoped features, G3/G5/G6 built.

**Final validation (post-implementation):** type-check 0 errors · eslint 0 errors/0 warnings · vitest 647 files / 7,581 tests passing (2 skipped) · combat+matchmaking suites (100 files / 1,498 tests) green — canonical combat behavior unchanged (house rules default to canonical).

---

## Phase 5 — Final Ledger

### Disposition table (finding → resolution → commit)

| Finding | Disposition | Commit |
|---------|-------------|--------|
| A1 duplicate tournament engine | Removed (superseded by `tournamentSelection/`) | `f71466bd` |
| B1 `Warrior.seasonPoints` | Wired: accrual, season reset, points-race line, WarriorDetail display | `47177b72` |
| D1 dead helpers | Removed with dead-code tests | `47177b72` |
| A2 `burnAnalysis` | Wired into `performAITraining` burned-attribute exclusion + `computeTrainability` surfaced in TrainingCardHeader | `3d7d853e` |
| A3 `tacticAdvisor` | Wired into plan generation (advisor-ranked tactics replace hardcoded switch) | `3d7d853e` |
| D2–D27 dead exports | Wired or dispositioned per row | `d388f724` |
| D3 `pickText` + ArenaHub fake chrome | Removed; ArenaHub chrome replaced with real-state values | `093f8709` |
| A4 `run-round/` results UI | Salvaged: `RunResults`/`BoutRow`/`RunResultsSummary`/`OutcomeIcon` into `BoutsStep` | `2a853647` |
| A5 `WeaponTrail` | Wired: weapon-id→trail classification, plumbed through `BoutViewer`→`ArenaView` + `MatchViewer`/`WarriorFightHistory` callers | `2a853647` |
| A6 UI primitives | `StatCard` deduped into WarriorDetail/StableDossier; `HeaderMetricDisplay` superseded (register-allowed) | `2a853647` |
| F1 stable notoriety | Wired into rival bout-offer hype | `40c31eb4` |
| F4 `fightForecast` | Wired: style-matchup edge gates rival bout acceptance | `2a58822b` |
| F2/F3 | NOTE — verified wired on own paths (`scouting.ts` consumer; `cumulativeTracker` harness-only) | — |
| C1–C5 dead data | Wired: persona→scout notes, arena lore→leaderboards, `getArenasByTag`→weather-aware matchmaking, meta quotes→doctrine intel, encumbrance labels→dossier | `09f1cddf` |
| E1–E4 hidden routes | Nav-wired: Hall of Fights→World, Physicals→Stable, Help/Admin/Import-Export/Mods→utility strip | `669c7533` |
| A7–A10 cleanup | Dead barrels + test-only combatFactory removed; `handleLocalStorageQuotaError` wired into array-persistence paths | `669c7533` |
| G3 a11y pack | Implemented: persisted contrast + text scale, boot-time apply, Help controls | `c683c3bd` |
| G5 bible search | Implemented: `bibleIndex` heading-section search on Help page | `c683c3bd` |
| G6 kill analytics | Implemented: persisted-field aggregator + "Mechanics of Death" Graveyard tab | `c683c3bd` |
| G4 onboarding quests | Implemented: GameState-derived checklist + `QuestsWidget` on Control Center | `71803c2e` |
| G2 import/export | Implemented: `/import-export` JSON+YAML packs, strict schema (exposed+fixed `absoluteWeek` drift) | `0cd89d1b` |
| G1 house rules | Implemented: `houseRules` levers (`deathRateMult`, `severeInjuryInsteadOfDeath`) + `/mods` page, non-canonical labeling | `c570a4f6` |
| G7 content updater | Implemented: persisted zod-validated content packs overlaying lore/quotes, `/mods` UI | `c570a4f6` |

### Before/after metrics

| Metric | Baseline (`49802cb2`) | Post-implementation |
|--------|--------------------:|--------------------:|
| Test files | 631 | 647 |
| Tests | 7,590 | 7,581 (2 skipped) |
| type-check errors | 0 | 0 |
| lint errors/warnings | 0/0 | 0/0 |
| Nav-hidden routes | 5 | 0 unexplained (`/arena-hub` is a registered duplicate alias, E5) |

Net test delta −9 with +16 files: deleted orphan-only tests for removed code (combatFactory, dead barrels, A1 tournament engine) and added ~40 wiring tests that exercise production entry paths (T2-compliant), not isolated units.

### Phase 5 validation battery

| Gate | Result |
|------|--------|
| `type-check` (router gen + tsc --build) | 0 errors |
| `eslint .` | 0 errors / 0 warnings |
| `bun x vitest run` | 647 files / 7,581 tests green (2 skipped) |
| `test:slow` (determinism, world liveness, trait balance, pipeline perf) | 10 files / 121 tests green |
| `narrative-validate` | clean |
| `build` (vite) | green — all new routes chunked |
| `electron:compile` | green |
| Headless soak (`scripts/soak.mjs`, 41 weeks / ~3 seasons, seed 20260919) | 4,397 bouts, 16 deaths, 7/9 intents active, dossier coverage 12.0, player challenged ×2, 169 traited warriors |
| `test:all` (full suite incl. slow) | 657 files / 7,702 tests green (2 skipped) |
| `e2e` (playwright ×5 projects) | 5/5 green (golden-path: new game → all pages → fight → advance week) |

Soak caveat: the harness drives `advanceWeek` only — tournament rounds resolve in `advanceDay`/`TickOrchestrator`, so tournament counts are zero in the soak by construction. Daily-path tournament resolution is covered by `TickOrchestrator.test.ts`, `dayPipeline.test.ts`, and the tournamentSelection suite.

### New seed expressions (RNG audit, §constraints)

No new RNG consumers added — `deathRateMult` scales an existing threshold (no extra draw), quests derive deterministically from state, packs/a11y prefs are UI-persisted. `determinism.slow.test.ts` (identical-pulse two-seed runs) green.

### Known residual items (deferred, documented)

- `cumulativeTracker` (F3): intentionally harness-only instrumentation — closed as NOTE.
- `/arena-hub` duplicate route (E5): registered alias of `/stable/arena`; no action.
- `_headless` ignored param at `RivalStrategyPass:302`: pre-existing, carried as V8 candidate per plan §93.
- Emergent-report instrumentation (`src/scripts/emergent-report.test.ts`): pre-existing diagnostic harness, unchanged.
