/**
 * Stable Hub Root Layout
 * Provides the layout for all Stable hub pages with sub-navigation
 */
import { Outlet } from '@tanstack/react-router';

/**
 *
 */
export default function StableLayout() {
  return <Outlet />;
}
